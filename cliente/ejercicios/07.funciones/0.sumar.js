// 1. Planteamiento

// inputs: a, b  (numbers)
// output: a + b (number)

// La función retorna la suma -> return

// 2. Lógica

// a y b -> a + b
// A partir de dos números calculamos la suma

// 3. Sintáxis 

/**
 * Pseudocódigo: 
 * 
 * function sumar (number a, number b){
 *  suma = a + b;
 *  return suma
 * }
 * 
 * Es un híbrido entre el planteamiento/lógica 
 * y el código final
 */

// https://jsdoc.app/about-getting-started
/**
 * Función que calcula la suma
 * @param {number} a 
 * @param {number} b 
 * @returns suma de dos números
 */
function sumar (a, b){
  let suma = a + b;
  return suma;
}
