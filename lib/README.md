```bash
# Añadir nuevo submódulo:
git submodule add <URL_repositorio.git> <ubicacion_relativa>

git submodule update --init --recursive
git submodule update --recursive

git submodule update --init <ubicacion>
git submodule status

git pull origin main

git submodule

# Actualizar submódulo:
git submodule update --init <ubicacion_relativa>
git submodule update --remote <ubicacion_relativa>
```